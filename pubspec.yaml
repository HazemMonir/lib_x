name: lib_x
description: lib_x is an Object-Oriented approach to flutter. It provides Routing, State Management & Data Provider Solution.
version: 0.0.5
homepage: https://github.com/HazemMonir/lib_x

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.2.1
  google_fonts: ^3.0.1
  overlay_support: ^2.1.0
  routemaster: ^1.0.1
  back_button_interceptor: ^6.0.2
  intl: ^0.17.0
  url_strategy: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

